#include <stdio.h>
/**
 * main - ...
 * @argc: type de compteur d'arguments int
 * @argv: strings qui contient des arguments
 * Return: ...
 */
int main(__attribute__((unused))int argc, char **argv)
{
	printf("%s\n", argv[0]);
return (0);
}
